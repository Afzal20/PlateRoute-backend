# backoffice owns no domain models (ARCHITECTURE §17: orchestrating views only).
# Sources of truth are administered via their owning apps' admin modules
# (orders.admin, payments.admin, common.admin).
